# 🔧 План исправления уязвимостей безопасности

## Этап 1: Криптографический модуль ⚡ СРОЧНО

### Создать `/src/utils/crypto.ts`
- ✅ AES-256-GCM шифрование для чувствительных данных
- ✅ HMAC-SHA256 для проверки целостности
- ✅ Безопасная генерация ключей (Web Crypto API)
- ✅ Шифрование PII данных (ФИО, телефоны, адреса)
- ✅ Шифрование финансовых данных

**Функции**:
```typescript
encryptData(data: any): Promise<string>
decryptData(encrypted: string): Promise<any>
generateHMAC(data: string): Promise<string>
verifyHMAC(data: string, hmac: string): Promise<boolean>
```

---

## Этап 2: Безопасная аутентификация ⚡ СРОЧНО

### Обновить `/src/utils/auth.ts`
- ✅ Убрать хардкод пароля
- ✅ Добавить хеширование паролей (Web Crypto API)
- ✅ Session timeout (30 минут неактивности)
- ✅ Token rotation
- ✅ Автоматический выход при истечении сессии

### Обновить `/src/app/components/LoginScreen.tsx`
- ✅ Удалить хардкод `password === 'owner'`
- ✅ Добавить rate limiting (5 попыток / 5 минут)
- ✅ Временная блокировка после неудачных попыток
- ✅ Очистка пароля из памяти после входа

**Новая система**:
```typescript
// Первый запуск - установить пароль
setMasterPassword(password: string): Promise<boolean>

// Вход
authenticateUser(password: string): Promise<boolean>

// Проверка сессии
isSessionValid(): boolean

// Обновление сессии
refreshSession(): void
```

---

## Этап 3: Rate Limiting ⚡ СРОЧНО

### Создать `/src/utils/rateLimiter.ts`
- ✅ Ограничение попыток входа (5 попыток / 5 минут)
- ✅ Временная блокировка IP/устройства
- ✅ Прогрессивное увеличение времени блокировки
- ✅ Сброс счетчика после успешного входа

**Функции**:
```typescript
checkRateLimit(action: string): Promise<boolean>
recordAttempt(action: string): void
resetAttempts(action: string): void
getBlockedUntil(action: string): Date | null
```

---

## Этап 4: Логирование безопасности 📅 ВАЖНО

### Создать `/src/utils/securityLogger.ts`
- ✅ Логирование входов/выходов
- ✅ Логирование изменений данных (CRUD)
- ✅ Логирование подозрительных действий
- ✅ Алерты на аномальную активность
- ✅ Экспорт логов в JSON

**Типы событий**:
```typescript
- LOGIN_SUCCESS
- LOGIN_FAILED
- LOGOUT
- DATA_CREATED
- DATA_UPDATED
- DATA_DELETED
- RATE_LIMIT_EXCEEDED
- SESSION_EXPIRED
- SUSPICIOUS_ACTIVITY
```

---

## Этап 5: Content Security Policy 📅 ВАЖНО

### Обновить `/index.html`
- ✅ Добавить CSP meta тег
- ✅ X-Content-Type-Options: nosniff
- ✅ X-Frame-Options: DENY
- ✅ Referrer-Policy: strict-origin

```html
<meta http-equiv="Content-Security-Policy" 
  content="default-src 'self'; 
           script-src 'self'; 
           style-src 'self' 'unsafe-inline'; 
           img-src 'self' https://images.unsplash.com data:; 
           connect-src 'self' https://wa.me;">
```

---

## Этап 6: Исправление dangerouslySetInnerHTML 📅 ВАЖНО

### Обновить `/src/app/components/ui/chart.tsx`
- ✅ Заменить dangerouslySetInnerHTML на безопасный способ
- ✅ Использовать CSS Modules или styled-components
- ✅ Валидация входных данных для CSS

**Варианты решения**:
1. Использовать CSS-in-JS библиотеку
2. Генерировать стили динамически через CSSStyleSheet API
3. Использовать CSS Custom Properties

---

## Этап 7: Проверка целостности данных 📋 ЖЕЛАТЕЛЬНО

### Обновить `/src/utils/safeStorage.ts`
- ✅ Добавить HMAC подпись для каждого элемента
- ✅ Проверка целостности при чтении
- ✅ Алерт при обнаружении подделки
- ✅ Автоматическая очистка поврежденных данных

**Новая структура**:
```typescript
{
  data: "encrypted_payload",
  hmac: "signature",
  timestamp: 1234567890,
  version: "1.0"
}
```

---

## Этап 8: GDPR Compliance 📋 ЖЕЛАТЕЛЬНО

### Создать `/src/utils/gdprCompliance.ts`
- ✅ Согласие на обработку ПД
- ✅ Право на удаление данных
- ✅ Право на экспорт данных
- ✅ Анонимизация удаленных данных
- ✅ Data retention policy

**Функции**:
```typescript
requestConsent(): Promise<boolean>
exportUserData(userId: string): Promise<Blob>
deleteUserData(userId: string, anonymize: boolean): Promise<void>
cleanupOldData(retentionDays: number): Promise<void>
```

---

## Этап 9: Дополнительные улучшения 📋 ЖЕЛАТЕЛЬНО

### 9.1 Версионирование данных
```typescript
// utils/dataVersioning.ts
migrateData(oldVersion: string, newVersion: string): any
validateSchema(data: any, schema: Schema): boolean
```

### 9.2 Резервное копирование
```typescript
// utils/backup.ts
createBackup(): Promise<Blob>
restoreBackup(file: File): Promise<boolean>
scheduleAutoBackup(intervalHours: number): void
```

### 9.3 Аудит-лог
```typescript
// utils/auditLog.ts
logAction(user: string, action: string, target: string): void
getAuditLog(filter: AuditFilter): AuditEntry[]
exportAuditLog(): Promise<Blob>
```

---

## 📊 Порядок внедрения (Roadmap)

### День 1 (Сегодня):
1. ✅ Создать crypto.ts - шифрование данных
2. ✅ Обновить auth.ts - убрать хардкод пароля
3. ✅ Создать rateLimiter.ts - защита от brute force
4. ✅ Обновить LoginScreen.tsx - rate limiting

### День 2:
5. ✅ Создать securityLogger.ts - логирование
6. ✅ Обновить App.tsx - интеграция логирования
7. ✅ Добавить CSP в index.html
8. ✅ Исправить chart.tsx (dangerouslySetInnerHTML)

### День 3:
9. ✅ Обновить safeStorage.ts - HMAC проверка
10. ✅ Добавить session timeout в auth.ts
11. ✅ Тестирование всех компонентов
12. ✅ Создать документацию

### Неделя 2:
13. GDPR compliance модуль
14. Data versioning
15. Backup system
16. Audit log

---

## 🎯 Метрики успеха

### До исправлений:
- OWASP Rating: ⚠️ СРЕДНИЙ
- Критических уязвимостей: 8
- CVSS средний: 7.5

### После исправлений:
- OWASP Rating: ✅ ХОРОШИЙ
- Критических уязвимостей: 0
- CVSS средний: < 3.0

---

## ✅ Чеклист внедрения

**Этап 1 - Криптография:**
- [ ] Создан crypto.ts
- [ ] Реализовано шифрование AES-256-GCM
- [ ] Реализован HMAC-SHA256
- [ ] Протестировано шифрование/дешифрование
- [ ] Интегрировано в safeStorage

**Этап 2 - Аутентификация:**
- [ ] Удален хардкод пароля
- [ ] Добавлено хеширование паролей
- [ ] Реализован session timeout
- [ ] Добавлен token rotation
- [ ] Протестирован вход/выход

**Этап 3 - Rate Limiting:**
- [ ] Создан rateLimiter.ts
- [ ] Добавлено ограничение попыток
- [ ] Реализована временная блокировка
- [ ] Интегрировано в LoginScreen
- [ ] Протестирована защита

**Этап 4 - Логирование:**
- [ ] Создан securityLogger.ts
- [ ] Добавлено логирование всех действий
- [ ] Реализованы алерты
- [ ] Протестирован экспорт логов

**Этап 5 - CSP:**
- [ ] Добавлен CSP в index.html
- [ ] Добавлены security headers
- [ ] Протестирована блокировка XSS

**Этап 6 - dangerouslySetInnerHTML:**
- [ ] Исправлен chart.tsx
- [ ] Удалено использование dangerouslySetInnerHTML
- [ ] Протестирована генерация стилей

**Этап 7 - Целостность:**
- [ ] Добавлена HMAC проверка
- [ ] Реализована валидация данных
- [ ] Протестирована защита от подделки

---

## 🚀 Начинаем внедрение!

Приступаем к реализации с **Этапа 1 - Криптографический модуль**.
