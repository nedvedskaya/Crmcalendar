# ✅ Чек-лист рефакторинга DRY

## 🎯 Цель
Устранить дублирование кода и привести проект к принципу DRY (Don't Repeat Yourself)

---

## 📦 Фаза 1: Подготовка (уже выполнено ✅)

- [x] Создан `/src/utils/constants.ts`
- [x] Создан `/src/utils/helpers.ts`
- [x] Создан `/src/utils/initialStates.ts`
- [x] Создан `/src/app/components/ui/Header.tsx`

---

## 🔧 Фаза 2: Рефакторинг App.tsx

### Шаг 2.1: Добавить импорты в начало файла
```typescript
// После строки 1, добавить:
import { 
  BTN_METAL, BTN_METAL_DARK, CARD_METAL,
  BRANCHES, TASK_URGENCY,
  CAR_DATABASE, CAR_ALIASES, CITIES_DATABASE,
  INITIAL_CLIENTS, INITIAL_TASKS, INITIAL_TRANSACTIONS, INITIAL_EVENTS
} from '@/utils/constants';
import { formatMoney, formatDate, getDateStr, generateId } from '@/utils/helpers';
import { 
  getInitialTaskState, getInitialRecordState, 
  getInitialClientState, getInitialTransactionState,
  getInitialCalendarEntryState 
} from '@/utils/initialStates';
```

### Шаг 2.2: Удалить дубликаты констант
- [ ] Удалить строки 14-24 (BRANCHES, TASK_URGENCY, BTN_*, CARD_METAL)
- [ ] Удалить строки 26-52 (CAR_DATABASE, CAR_ALIASES)
- [ ] Удалить строку 54 (CITIES_DATABASE)
- [ ] Удалить строки 56-59 (INITIAL_*)

### Шаг 2.3: Удалить дубликаты функций
- [ ] Удалить строки 63-67 (getDateStr)
- [ ] Удалить строки 69-72 (formatMoney)
- [ ] Удалить строки 74-79 (formatDate)

### Шаг 2.4: Заменить компонент Header
- [ ] Удалить строки 208-223 (определение Header)
- [ ] Добавить импорт: `import { Header } from '@/app/components/ui/Header';`
- [ ] Убедиться что все использования Header корректны (уже используют правильные пропсы)

### Шаг 2.5: Заменить инициализации состояний

**ClientForm (строка ~380):**
- [ ] Заменить `{ createdAt: getDateStr(0), name: '', phone: '+', ... }` на `getInitialClientState()`

**ClientForm taskInput (строка ~385):**
- [ ] Заменить `{ title: '', date: getDateStr(0), time: '12:00', isUrgent: false }` на `getInitialTaskState()`

**ClientForm - после добавления задачи (строка ~455):**
- [ ] Заменить `{title: '', date: getDateStr(0), time: '12:00', isUrgent: false}` на `getInitialTaskState()`

**ClientDetails newTask (строка ~485):**
- [ ] Заменить на `getInitialTaskState(client.branch)`

**ClientDetails newRecord (строка ~488):**
- [ ] Заменить на `getInitialRecordState()`

**ClientDetails - после сохранения задачи (строки ~521, 527):**
- [ ] Заменить на `getInitialTaskState(client.branch)`

**ClientDetails - после сохранения записи (строки ~562, 577):**
- [ ] Заменить на `getInitialRecordState()`

**TasksView newTask (строка ~759):**
- [ ] Заменить на `getInitialTaskState()`

**TasksView - после добавления/редактирования (строки ~815, 829):**
- [ ] Заменить на `getInitialTaskState()`

**CalendarView newEntry (строка ~1056):**
- [ ] Заменить на `getInitialCalendarEntryState()`

---

## 🔧 Фаза 3: Рефакторинг FinanceView.tsx

### Шаг 3.1: Добавить импорты
```typescript
// После строки 1, добавить:
import { BTN_METAL_DARK } from '@/utils/constants';
import { formatMoney } from '@/utils/helpers';
import { getInitialTransactionState } from '@/utils/initialStates';
import { Header } from '@/app/components/ui/Header';
```

### Шаг 3.2: Удалить дубликаты
- [ ] Удалить строку 4 (BTN_METAL_DARK)
- [ ] Удалить строки 6-9 (formatMoney)
- [ ] Удалить строки 11-20 (Header)

### Шаг 3.3: Обновить использование Header
- [ ] Найти `<Header title="Финансы" ...` (строка ~?)
- [ ] Добавить проп `variant="simple"`

### Шаг 3.4: Заменить инициализацию состояния
- [ ] Строка ~26: заменить `{ title: '', amount: '', type: 'income', sub: '' }` на `getInitialTransactionState()`

---

## 🧪 Фаза 4: Тестирование

### Функциональные тесты
- [ ] Запустить приложение без ошибок
- [ ] Проверить добавление клиента
- [ ] Проверить создание задачи
- [ ] Проверить добавление записи на услугу
- [ ] Проверить финансовую секцию
- [ ] Проверить календарь
- [ ] Проверить переключение веток МСК/РНД

### Визуальные тесты
- [ ] Проверить что все кнопки выглядят корректно
- [ ] Проверить что Header отображается правильно на всех экранах
- [ ] Проверить что форматирование денег работает

---

## 📊 Метрики успеха

### До рефакторинга:
- **Дублирование функций:** 3 случая (formatMoney, formatDate, getDateStr)
- **Дублирование констант:** 2 случая (BTN_METAL_DARK, Header)
- **Повторяющиеся инициализации:** 15+ случаев
- **Строк дублированного кода:** ~200

### После рефакторинга:
- **Дублирование функций:** 0 ✅
- **Дублирование констант:** 0 ✅
- **Повторяющиеся инициализации:** 0 ✅
- **Строк дублированного кода:** 0 ✅
- **Сэкономлено строк:** ~150-200

---

## 🎉 Финальная проверка

- [ ] Все импорты работают корректно
- [ ] Нет TypeScript/ESLint ошибок
- [ ] Приложение компилируется
- [ ] Все функции работают как раньше
- [ ] Код стал чище и понятнее
- [ ] Легче вносить изменения в будущем

---

## ⚠️ Возможные проблемы

### Проблема 1: TypeScript ошибки типов
**Решение:** В `helpers.ts` можно временно использовать `any`, потом добавить proper types

### Проблема 2: Circular dependencies
**Решение:** Убедитесь что utils не импортируют компоненты, только наоборот

### Проблема 3: Изменение поведения
**Решение:** Тщательно проверьте что все функции возвращают тот же результат

---

## 📝 Примечания

- Рефакторинг можно делать постепенно (файл за файлом)
- Начните с самых простых изменений (импорты констант)
- Тестируйте после каждого большого изменения
- Используйте Git для отката в случае проблем

**Удачи! 🚀**
